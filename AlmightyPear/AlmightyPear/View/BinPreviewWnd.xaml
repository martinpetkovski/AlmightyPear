<Window x:Class="AlmightyPear.View.BinPreviewWnd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
        xmlns:local="clr-namespace:AlmightyPear.View"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Focusable="False"
        Topmost="True"
        Height="300"
        Width="300"
        Background="Transparent"
        Title="ImagePreviewWnd">
    <Grid Background="Transparent"  
          HorizontalAlignment="Center"
          VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" MaxWidth="300"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" MaxHeight="300"></RowDefinition>
        </Grid.RowDefinitions>
        <Image 
            Source="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=Model.ImageUrl}" 
            MaxWidth="300" MaxHeight="300"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>
    </Grid>
</Window>
