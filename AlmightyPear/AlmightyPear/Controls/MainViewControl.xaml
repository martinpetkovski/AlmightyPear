<UserControl x:Class="AlmightyPear.Controls.MainViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AlmightyPear.Controls"
             xmlns:controller="clr-namespace:AlmightyPear.Controller"
             xmlns:controls="clr-namespace:AlmightyPear.Controls"
             xmlns:model="clr-namespace:AlmightyPear.Model"
             xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <controller:Env x:Key="Env"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\Resources\Styles\BasicTheme.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="{StaticResource SolidDarkMono}" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="40,0,40,0">
                <TextBlock Text="Logged in as:  "></TextBlock>
                <TextBlock Text="{Binding Source={StaticResource Env}, Path=UserData.Email}"></TextBlock>
            </StackPanel>
            <Grid Grid.Column="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Name="btn_LogOut" 
                        Click="Btn_LogOut_Click" 
                        Margin="10,0,0,0" 
                        Padding="5" 
                        BorderBrush="#333" 
                        Grid.Column="0"
                        VerticalAlignment="Center">Log out</Button>
                <Separator Grid.Column="1"></Separator>
                <controls:SaveAllControl Grid.Column="2" Margin="10,0,0,0" CreateBookmarkControl="{x:Reference ctrl_CreateBookmark}"></controls:SaveAllControl>
                <Button Name="btn_ClearTempBin" 
                        Click="Btn_ClearTempBin_Click" 
                        Margin="10,0,0,0"
                        Padding="5" 
                        BorderBrush="#500" 
                        Grid.Column="3"
                        VerticalAlignment="Center">Clear Temp Bin</Button>
                
            </Grid>
            <Image Grid.Column="2" Source="../Resources/Textures/logo.png" Height="48" Margin="10"/>

        </Grid>
        <Border Grid.Row="1" Background="{StaticResource GradInsetSide}">
            <TabControl  TabStripPlacement="Left">
                <TabItem Header="{Binding Source={StaticResource Env}, Path=BinData.BookmarksViewCaption}">
                    <Grid>
                        <controls:BookmarksViewControl MaxContentHeight="10000" ContentTextSize="16"></controls:BookmarksViewControl>
                    </Grid>
                </TabItem>
                <TabItem Header="{Binding Source={StaticResource Env}, Path=BinData.BookmarksCreateCaption}">
                    <controls:BookmarkCreateControl x:Name="ctrl_CreateBookmark"></controls:BookmarkCreateControl>
                </TabItem>
                <TabItem Header="Profile"></TabItem>
            </TabControl>
        </Border>
        
    </Grid>
</UserControl>
